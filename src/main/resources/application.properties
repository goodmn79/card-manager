spring.application.name=card-manager

spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=validate

spring.liquibase.enabled=true
spring.liquibase.change-log=classpath:/db/changelog/db.changelog-master.yml
spring.liquibase.default-schema=public

initial.admin.username=${ADMIN_USERNAME}
initial.admin.password=${ADMIN_PASSWORD}
initial.admin.first_name=${ADMIN_FIRST_NAME}
initial.admin.midl_name=${ADMIN_MIDL_NAME}
initial.admin.last_name=${ADMIN_LAST_NAME}

spring.data.web.pageable.default-page-size=5
spring.data.web.pageable.max-page-size=20

jwt.secret=${JWT_SECRET}
jwt.expiration=86400000

jasypt.encryptor.password=${JASYPT_ENCRYPTOR_PASSWORD}
